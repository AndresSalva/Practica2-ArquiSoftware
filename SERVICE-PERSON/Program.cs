using System; /*borrar  despues*/

namespace ServicePerson
{
    public class Program
    {
        public static void Main(string[] args)
        {
            Console.WriteLine("Servicio PERSON ejecutándose correctamente...");

            // Aquí puedes probar tu código manualmente.
            // Por ejemplo, crear un repositorio o llamar un método de prueba.
            // Ejemplo:
            // var repo = new PersonRepository();
            // var personas = repo.GetAll();
            // Console.WriteLine($"Se encontraron {personas.Count()} personas.");
        }
    }
}